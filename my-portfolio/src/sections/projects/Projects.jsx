import { ProjectCard } from "../../components/ProjectCard";

//map through all the projects in json
export const Projects = () => {
    return (
        <div>
            < ProjectCard />
        </div>
    )
}